version: '3.8'

services:
  neurofuzzy-screening:
    build: .
    container_name: neurofuzzy_screening_service
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    restart: unless-stopped
